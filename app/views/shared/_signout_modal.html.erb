<% if current_user %>
  <div class="modal fade" id="signoutModal" tabindex="-1" aria-labelledby="signoutModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header border-0">
          <h3 class="modal-title" id="signoutModalLabel"><%= t('sessions.sign_out_modal_title') %></h3>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <hr class="m-0 text-danger">
        <div class="modal-body">
          <p><%= t('sessions.sign_out_confirmation') %></p>
        </div>
        <div class="modal-footer justify-content-between border-0">
          <button type="button" class="btn btn-outline-danger" data-bs-dismiss="modal"><%= t('sessions.sign_out_cancel') %></button>
          <%= link_to t('forms.sign_out'), '#', method: :delete, class: "btn btn-danger", data: { turbo_method: :delete } %>
        </div>
      </div>
    </div>
  </div>
<% end %>